<!DOCTYPE html>
<html>
  <head>
		<title> Category: {{ data.category }} </title>
    <meta charset="utf-8">
		<meta name="author" content="Donghee Jeong">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/github-markdown.css">
  </head>

  <style>
    .markdown-body {
      box-sizing: border-box;
      min-width: 200px;
      max-width: 980px;
      margin: 0 auto;
      padding: 45px;
    }
    @media (max-width: 767px) {
      .markdown-body {
	padding: 15px;
      }
    }
  </style>

  <article class="markdown-body">
		<h1> Category: {{ data.category }} </h1>

		{% if data.articles is defined and data.articles|length %}
    <ul>
			{%- for article in data.articles %}
			<li><small>{{ article.metadata.date[0][1:-1] }}</small> <a href="/{{ article.category }}/{{ article.fname | replace(".md", ".html") }}">{{ article.metadata.title[0][1:-1] }}</a></li>
			{% endfor %}
    </ul>
		{% else %}
    No articles exist
		{% endif %}
  </article>
  <p class="credit markdown-body">
    <small>This website uses <a href="https://github.com/hyrious/github-markdown-css">hyrious/github-markdown-css</a> stylesheet.</small><br>
    <small><a href="/">Go to Home</a></small><br>
  </p>
</html>
